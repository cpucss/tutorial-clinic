import { useMemo, useState } from "react";
import { Pencil, Plus, Search, Trash2, X } from "lucide-react";
import { ConfirmDialog, EmptyState, InlineNotice, StatusBadge } from "../../../components/common/Feedback";
import type { ToastMessage } from "../../../components/common/Feedback";
import { useAppData } from "../../../context/AppDataContext";
import type { Subject } from "../../../types/app";
import type { YearLevel } from "../../../types/common";

export function AdminSubjectsPage({ onNotify }: { onNotify?: (toast: Omit<ToastMessage, "id">) => void }) {
  const { state, deleteSubject } = useAppData(); const [query, setQuery] = useState(""); const [year, setYear] = useState("All"); const [status, setStatus] = useState("All"); const [editor, setEditor] = useState<Subject | "new" | null>(null); const [remove, setRemove] = useState<Subject | null>(null);
  const subjects = useMemo(() => state.subjects.filter((subject) => (!query || `${subject.code} ${subject.name} ${subject.coordinator}`.toLowerCase().includes(query.toLowerCase())) && (year === "All" || subject.yearLevel === year) && (status === "All" || (status === "Active" ? subject.active : !subject.active))).sort((a, b) => a.code.localeCompare(b.code)), [query, state.subjects, status, year]);
  return <div className="h-full overflow-y-auto"><div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-10 lg:py-8"><header className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="section-kicker">Curriculum setup</div><h1 className="page-heading">Subjects</h1><p className="page-description">Maintain the official subject catalog used by sessions and student note uploads.</p></div><button className="primary-button" onClick={() => setEditor("new")}><Plus size={15} /> Add subject</button></header><section className="mt-6 grid gap-3 rounded-xl bg-white p-4 demo-card md:grid-cols-[1fr_180px_180px]"><label className="search-field"><Search size={15} /><span className="sr-only">Search subjects</span><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search code, name, or coordinator" /></label><label className="compact-field"><span>Year</span><select value={year} onChange={(e) => setYear(e.target.value)}>{["All", "Freshman", "Sophomore", "Junior", "Senior"].map((item) => <option key={item}>{item}</option>)}</select></label><label className="compact-field"><span>Status</span><select value={status} onChange={(e) => setStatus(e.target.value)}>{["All", "Active", "Inactive"].map((item) => <option key={item}>{item}</option>)}</select></label></section><section className="mt-5 overflow-hidden rounded-xl bg-white demo-card"><div className="table-scroll"><table className="data-table"><thead><tr><th>Code</th><th>Subject</th><th>Year</th><th>Coordinator</th><th>Usage</th><th>Actions</th></tr></thead><tbody>{subjects.map((subject) => { const sessions = state.events.filter((event) => event.subjectId === subject.id).length; const notes = state.notes.filter((note) => note.subjectId === subject.id).length; return <tr key={subject.id}><td><strong className="text-[#9A5D0B]">{subject.code}</strong></td><td><strong>{subject.name}</strong>{!subject.active && <StatusBadge status="Inactive" />}</td><td>{subject.yearLevel}</td><td>{subject.coordinator}</td><td>{sessions} sessions<small>{notes} notes</small></td><td><div className="table-actions"><button onClick={() => setEditor(subject)}><Pencil size={14} /> Edit</button><button className="danger-text" onClick={() => setRemove(subject)}><Trash2 size={14} /> Delete</button></div></td></tr>; })}</tbody></table></div>{!subjects.length && <div className="p-5"><EmptyState title="No subjects found" body="Change the filters or add a subject." /></div>}</section></div>{editor && <SubjectEditor subject={editor === "new" ? undefined : editor} onClose={() => setEditor(null)} onNotify={onNotify} />}<ConfirmDialog open={Boolean(remove)} title="Delete this subject?" body={`${remove?.code ?? "This subject"} can only be deleted when no session or note uses it.`} confirmLabel="Delete subject" cancelLabel="Keep subject" tone="error" onCancel={() => setRemove(null)} onConfirm={() => { if (remove) { const result = deleteSubject(remove.id); onNotify?.({ tone: result.ok ? "warning" : "error", title: result.ok ? "Subject deleted" : "Subject cannot be deleted", description: result.ok ? `${remove.code} was removed.` : result.message }); } setRemove(null); }} /></div>;
}
function SubjectEditor({ subject, onClose, onNotify }: { subject?: Subject; onClose: () => void; onNotify?: (toast: Omit<ToastMessage, "id">) => void }) { const { saveSubject } = useAppData(); const [code, setCode] = useState(subject?.code ?? ""); const [name, setName] = useState(subject?.name ?? ""); const [yearLevel, setYearLevel] = useState<YearLevel>(subject?.yearLevel ?? "Freshman"); const [coordinator, setCoordinator] = useState(subject?.coordinator ?? ""); const [active, setActive] = useState(subject?.active ?? true); const [error, setError] = useState(""); function submit() { const result = saveSubject({ ...subject, code, name, yearLevel, coordinator, active }); if (!result.ok) { setError(result.message); return; } onNotify?.({ tone: "success", title: subject ? "Subject updated" : "Subject added", description: `${code.toUpperCase()} is available throughout the application.` }); onClose(); } return <div className="confirm-overlay" onMouseDown={onClose}><div className="entity-detail-dialog" role="dialog" aria-modal="true" onMouseDown={(e) => e.stopPropagation()}><header><div><div className="section-kicker">{subject ? "Edit subject" : "New subject"}</div><h2>{subject?.code ?? "Add curriculum subject"}</h2></div><button className="icon-button rounded-full bg-[#FAF8F2]" onClick={onClose}><X size={16} /></button></header>{error && <InlineNotice tone="error" title="Subject not saved">{error}</InlineNotice>}<div className="entity-form-grid mt-5"><label className="form-field"><span>Subject code</span><input value={code} onChange={(e) => setCode(e.target.value)} /></label><label className="form-field"><span>Year level</span><select value={yearLevel} onChange={(e) => setYearLevel(e.target.value as YearLevel)}>{["Freshman", "Sophomore", "Junior", "Senior"].map((item) => <option key={item}>{item}</option>)}</select></label><label className="form-field md:col-span-2"><span>Subject name</span><input value={name} onChange={(e) => setName(e.target.value)} /></label><label className="form-field md:col-span-2"><span>Coordinator</span><input value={coordinator} onChange={(e) => setCoordinator(e.target.value)} /></label><label className="check-chip"><input type="checkbox" checked={active} onChange={(e) => setActive(e.target.checked)} />Active subject</label></div><footer><button className="secondary-button" onClick={onClose}>Cancel</button><button className="primary-button" onClick={submit}>Save subject</button></footer></div></div>; }
