import { BookMarked, CheckCircle2 } from "lucide-react";
const rules = [{ action: "Administrator-approved session attendance", points: "+40" }, { action: "Administrator-approved study note", points: "+60" }, { action: "Transparent administrator adjustment", points: "Varies" }];
export function PointsPage() { return <div className="h-full overflow-y-auto"><div className="mx-auto max-w-4xl px-4 py-6 sm:px-6 lg:px-10 lg:py-8"><div className="section-kicker">Program guide</div><h1 className="page-heading">How points work</h1><p className="page-description">Tutorial Clinic points recognize verified participation and useful contributions. Every change appears in Point History.</p><section className="mt-6 rounded-xl bg-white p-5 demo-card"><div className="flex items-center gap-2"><BookMarked color="#F5A623" /><h2 className="text-lg font-bold">Earning points</h2></div><ul className="mt-4 grid gap-2">{rules.map((rule) => <li className="history-row" key={rule.action}><span>{rule.action}</span><strong className="text-[#9A5D0B]">{rule.points}</strong></li>)}</ul></section><section className="mt-5 grid gap-3 sm:grid-cols-2">{["Attendance must be approved before points are awarded.", "A rejected or pending note does not award points.", "Duplicate attendance records are prevented.", "Admin point adjustments always require a reason.", "The leaderboard and student profiles share one transaction source.", "Points remain available on this device after refreshing."].map((item) => <div className="flex gap-3 rounded-xl bg-white p-4 demo-card" key={item}><CheckCircle2 size={17} color="#F5A623" /><span className="text-sm">{item}</span></div>)}</section></div></div>; }
